import type {Metadata} from 'next';import {articleThumbnail} from '../article-thumbnails';import {Header,Footer} from '../components';import {alternativePages} from '../alternatives-data';
export const metadata:Metadata={title:'Developer Staffing Alternatives',description:'Independent comparisons of developer talent platforms, outsourcing firms, and Philippines-based offshore staffing.',alternates:{canonical:'https://developeroffshore.com/alternatives'}};
export default function Alternatives(){return <><Header/><main><section className="fleet-hero variant-2"><div className="container"><p className="eyebrow">Alternatives</p><h1>Compare developer staffing options by operating model</h1><p className="lead">Use current vendor sources, then verify contracts, assessments, locations, access controls, and day-to-day ownership.</p></div></section><section className="section"><div className="container fleet-service-grid">{alternativePages.map(page=>{const image=articleThumbnail('alternatives',page.slug,page.title);return <a className="card article-card" href={`/alternatives/${page.slug}`} key={page.slug}><img src={image.src} alt={image.alt}/><p className="eyebrow">{page.competitor} comparison</p><h2>{page.title}</h2><p>{page.description}</p><b>Read comparison →</b></a>})}</div></section></main><Footer/></>}
