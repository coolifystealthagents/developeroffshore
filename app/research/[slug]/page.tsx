import type {Metadata} from 'next';
import {notFound} from 'next/navigation';
import {articleThumbnail} from '../../article-thumbnails';
import {Header,Footer} from '../../components';
import {researchPosts,ResearchPost} from '../../fleet-data';
import {site} from '../../data';

export function generateStaticParams(){return researchPosts.map(p=>({slug:p.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const post=researchPosts.find(p=>p.slug===slug);if(!post)return{};const thumbnail=articleThumbnail('research',post.slug,post.title);const baseUrl=`https://${site.domain.toLowerCase()}`;const url=`${baseUrl}/research/${post.slug}`;return{title:post.title,description:post.excerpt,alternates:{canonical:url},openGraph:{title:post.title,description:post.excerpt,url,type:'article',images:[{url:`${baseUrl}${thumbnail.src}`,alt:thumbnail.alt}]}}}
export default async function ResearchArticle({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const post:ResearchPost|undefined=researchPosts.find(p=>p.slug===slug);if(!post)notFound();const thumbnail=articleThumbnail('research',post.slug,post.title);const canonical=`https://${site.domain.toLowerCase()}/research/${post.slug}`;const faqSchema=post.faqs.map(f=>({"@type":"Question",name:f.question,acceptedAnswer:{"@type":"Answer",text:f.answer}}));return <><Header/><main><article className="section"><div className="container article-shell"><p className="eyebrow">{site.brand} research</p><h1>{post.title}</h1><p className="article-date"><time dateTime={post.published}>{post.published}</time> · Research report</p><p className="lead">{post.excerpt}</p><p>Use this report with the <a href="/research">Research library</a> and the related <a href="/blog">daily developer guides</a> to turn evidence into a bounded work brief.</p><img className="article-featured-image" src={thumbnail.src} alt={thumbnail.alt}/><section className="card"><h2>Key Stats</h2><ul>{post.keyStats.map(stat=><li key={stat}>{stat}</li>)}</ul></section><section className="card"><h2>Key Takeaways</h2><ul>{post.takeaways.map(takeaway=><li key={takeaway}>{takeaway}</li>)}</ul></section>{post.sections.map(s=><section className="card" key={s.heading}><h2>{s.heading}</h2>{s.body.map(paragraph=><p key={paragraph}>{paragraph}</p>)}</section>)}<section className="card"><h2>Evidence table</h2><table><thead><tr><th>Signal</th><th>What to inspect</th><th>Owner</th></tr></thead><tbody><tr><td>Outcome</td><td>Acceptance evidence for the bounded task</td><td>Task reviewer</td></tr><tr><td>Control</td><td>Access, test, and approval boundary</td><td>Internal owner</td></tr><tr><td>Handoff</td><td>Open risks and next decision</td><td>Next owner</td></tr></tbody></table><blockquote>Good distributed work is observable at the handoff: the result, evidence, limitations, and next owner are all explicit.</blockquote></section><section className="card"><h2>Frequently asked questions</h2>{post.faqs.map(faq=><div key={faq.question}><h3>{faq.question}</h3><p>{faq.answer}</p></div>)}</section>{post.sources?.length?<section className="card sources-card"><h2>Sources</h2><ol>{post.sources.map(s=><li key={s.url}><a href={s.url} rel="noreferrer">{s.name}</a></li>)}</ol></section>:null}<section className="card"><h2>Related Research</h2><ul>{post.related.slice(0,3).map(link=><li key={link.href}><a href={link.href}>{link.title}</a></li>)}</ul></section><aside className="card"><h2>Apply the findings</h2><p>Turn this research into a bounded role, review owner, and first-week routine for your team.</p><p><a className="btn primary" href="/contact-us">Discuss a role</a> <a className="btn secondary" href="/blog">Read related blog guides</a></p></aside></div></article></main><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify([{"@context":"https://schema.org","@type":"ResearchArticle",headline:post.title,datePublished:post.published,dateModified:post.published,mainEntityOfPage:canonical,url:canonical,description:post.excerpt,author:{"@type":"Organization",name:site.brand},citation:post.sources?.map(s=>s.url)}, {"@context":"https://schema.org","@type":"FAQPage",mainEntity:faqSchema},{"@context":"https://schema.org","@type":"BreadcrumbList",itemListElement:[{"@type":"ListItem",position:1,name:"Research",item:`https://${site.domain}/research`},{"@type":"ListItem",position:2,name:post.title,item:canonical}]}])}}/><Footer/></>}
