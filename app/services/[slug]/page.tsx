import { Header, Footer, CTA } from '../../components';
import { services, site } from '../../data';
export function generateStaticParams(){return services.map(s=>({slug:s.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=services.find(x=>x.slug===slug);return {title:s?.title||'Service',description:s?.desc}}
export default async function Service({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=services.find(x=>x.slug===slug)||services[0];return <><Header/><main><section className='service-hero'><div className='container two'><div><p className='eyebrow'>Offshore developer support</p><h1>{s.title}</h1><p className='lead'>{s.desc}</p><a className='btn' href='/contact'>Talk through this role</a></div><div className='hero-card'><img src={site.serviceImage} alt={`${s.title} offshore service team`}/></div></div></section><section className='section'><div className='container cards'><div className='card'><h3>Good first tasks</h3><ul><li>Tickets with a clear result and an example</li><li>Bug fixes, tests, documentation, and small features</li><li>Work your tech lead can review in a pull request</li></ul></div><div className='card'><h3>How to check the work</h3><ul><li>Daily written update</li><li>Tests or screenshots with each pull request</li><li>A weekly review with your tech lead</li></ul></div><div className='card'><h3>First-week setup</h3><ul><li>Limited repo and ticket access</li><li>One sample ticket</li><li>Clear merge and escalation rules</li></ul></div></div></section><CTA/></main><Footer/></>}
